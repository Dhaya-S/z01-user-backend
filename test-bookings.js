const { Pool } = require('pg'); const pool = new Pool({ connectionString: process.env.DATABASE_URL || 'postgres://neondb_owner:npg_1s6cWhqYlPbe@ep-bitter-dust-a1i8s5t1-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require' }); async function run() { const res = await pool.query('SELECT * FROM bookings ORDER BY created_at DESC LIMIT 5'); console.log('Bookings:', res.rows); pool.end(); } run();
